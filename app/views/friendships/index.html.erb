<p id="notice"><%= notice %></p>

<h1>Friendships</h1>

<h2>[Debug]</h2>
<%= "This table is for debugging purposes only" %>

<table>
  <thead>
    <tr>
      <th>User</th>
      <th>Friend</th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @friendships.each do |friendship| %>
      <tr>
        <td><%= User.find(friendship.user_id).username %></td>
        <td><%= User.find(friendship.friend_id).username %></td>
      </tr>
    <% end %>
  </tbody>

</table>
